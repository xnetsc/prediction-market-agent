# Retired native helper output

This ignored-output directory may contain locally generated historical executables.
It is no longer copied into images or served by the application. Do not commit its
contents. Terminal helper sources are packaged from
`src/prediction_market_agent/plugins/providers/login_scripts/`.
